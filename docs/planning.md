# Planning

Language commands become high-level actions, then a bimanual coordinator validates the sequence. Collision checking is currently an explicit boundary and must be connected to MuJoCo geometry queries before learned policy deployment.
